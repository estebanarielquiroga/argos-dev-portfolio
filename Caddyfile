:{$PORT:8000} {
    root * /app/.web/build/client
    
    # Servimos archivos estaticos si existen
    file_server {
        pass_thru
    }
    
    # Si no es un archivo estatico (como /_event), va al backend
    reverse_proxy 127.0.0.1:8001
}
